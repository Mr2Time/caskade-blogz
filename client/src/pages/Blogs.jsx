import React from 'react';
import Create from '../components/Create';

const Blogs = ({data}) => {
    return (
        <div>
            <Create />
            All blogs page
        </div>
    );
}

export default Blogs;
